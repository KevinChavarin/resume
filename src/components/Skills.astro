---
import Translate from "./Translate";

type Skills = {
  skills: string[];
};

const skills: Skills = {
  skills: [
    "JavaScript / TypeScript",
    "React / Next.js",
    "Node.js",
    "Express.js",
    "HTML",
    "CSS / Tailwind CSS",
    "Git / GitHub",
    "Docker",
    "MySQL",
    "MongoDB",
    "Astro",
    "PHP",
    "Railway",
  ],
};

const softSkills = [
  "softSkills.problemSolving",
  "softSkills.teamwork",
  "softSkills.adaptability",
  "softSkills.timeManagement",
  "softSkills.criticalThinking",
  "softSkills.creativity",
  "softSkills.metaCommunication",
  "softSkills.growthMindset",
  "softSkills.attentionToDetail",
];

---

<section class="flex flex-wrap gap-2 max-w-4xl w-full mb-8 mt-2">
    {
      skills.skills.map((skill) => (
        <p class="flex px-2 py-1 text-sm rounded bg-white/50 dark:bg-white/10 backdrop-blur-sm text-gray-800 dark:text-neutral-200 border border-white/30 dark:border-white/10 hover:bg-white/70 dark:hover:bg-white/20 transition-colors">{skill}</p>
      ))
    }
</section>
<div text-2xl mb-2>
    <Translate
      textKey="softSkills.label"
      fallback="Soft Skills"
      client:load
    />
</div>
<section class="flex flex-wrap gap-2 max-w-4xl w-full mb-8 mt-2">
    {
        softSkills.map((skill) => (
          <p class="flex px-2 py-1 text-sm rounded bg-white/50 dark:bg-white/10 backdrop-blur-sm text-gray-800 dark:text-neutral-200 border border-white/30 dark:border-white/10 hover:bg-white/70 dark:hover:bg-white/20 transition-colors">
            <Translate
                textKey={skill}
                fallback={skill.split('.').pop()}
                client:load
            /> 
          </p>
            
        ))
    }
</section>
